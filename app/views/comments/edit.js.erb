var index = ('<%=@comment.id%>'); 
$('#comment-' + index + ' > .comment-inner .comment-form-number-' + index).remove();
$('#comment-' + index + ' > .comment-inner > .detail').after('<%= j (render 'form', comment: @comment, index: @comment.id) %>')